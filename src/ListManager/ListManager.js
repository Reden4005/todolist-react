import React, { Component } from "react";

class ListManager {
    state = {
        listTitle: "",
        listTasks: []
    }

    render() {
        return(
            
        )
    }
}

export default ListManager;